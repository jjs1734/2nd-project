<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wizian.web.dao.BoardDAO">

    <!-- Board 조회 쿼리 -->
    <select id="board" parameterType="int" resultType="com.wizian.web.dto.BoardDTO">
        SELECT BBS_SN, PST_SN, PST_CAT, PST_TTL, WRITER, PSTG_YMD, MDFCN_YMD, PST_COMP, BBS_SN
        FROM ICOUN_POSTS
        WHERE BBS_SN = #{boardNo} AND PST_DEL = '1'
    </select>
    
    <!-- 상세 조회 쿼리 -->
    <select id="detail" parameterType="int" resultType="com.wizian.web.dto.BoardDTO">
        SELECT PST_SN, PST_CAT, PST_TTL, PST_CN, WRITER, WRITER_ID, PSTG_YMD, MDFCN_YMD, PST_COMP, BBS_SN
        FROM ICOUN_POSTS
        WHERE PST_SN = #{no}
    </select>

    <!-- 글쓰기 쿼리 -->
    <insert id="write" parameterType="java.util.Map">
        INSERT INTO ICOUN_POSTS (PST_TTL, PST_CN,COUNSL WRITER_ID, PST_CAT, BBS_SN)
        VALUES (#{title}, #{content}, #{writerId}, #{cat}, #{boardNo})
    </insert>

</mapper>
